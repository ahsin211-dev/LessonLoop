{
  "name": "lessonloop-etl-api",
  "version": "1.0.0",
  "description": "LessonLoop backend API — Student Engagement Survey scoring and reports",
  "private": true,
  "scripts": {
    "start": "serverless offline start --stage local",
    "deploy": "serverless deploy",
    "test": "node --test test/**/*.test.js",
    "test:integration": "node scripts/integration-test.js",
    "seed": "node scripts/seed-local.js",
    "setup:local": "node scripts/setup-local-tables.js"
  },
  "dependencies": {
    "@aws-sdk/client-bedrock-runtime": "^3.1075.0",
    "@aws-sdk/client-dynamodb": "^3.700.0",
    "@aws-sdk/lib-dynamodb": "^3.700.0",
    "@opensearch-project/opensearch": "^2.13.0",
    "aws-jwt-verify": "^4.0.1",
    "jsonwebtoken": "^9.0.3",
    "uuid": "^11.0.3"
  },
  "devDependencies": {
    "serverless": "^3.39.0",
    "serverless-offline": "^13.8.1"
  },
  "engines": {
    "node": ">=20"
  }
}
